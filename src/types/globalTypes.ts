export interface IBook {
  _id: string;
  title: string;
  author: string;
  genre: string;
  publicationDate: string;
}

export interface option {
  value: string | number;
  label: string | number;
}
